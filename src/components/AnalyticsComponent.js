import LineGraph from "./LineGraph"

const AnalyticsComponent = ({ type }) => {
    return (
        <div>
            <LineGraph type={type} />
        </div>
    )
}

export default AnalyticsComponent